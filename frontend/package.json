{
  "name": "mpe-llm-frontend",
  "private": true,
  "version": "0.1.3",
  "description": "Self-contained React frontend for LLM sidecar plugin (config panels + execution viewer)",
  "type": "commonjs",
  "scripts": {
    "build": "vite build && vite build --config vite.config.viewer.ts && node scripts/inline.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
